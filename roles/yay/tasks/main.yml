- name: Upgrade packages
  kewlfft.aur.aur:
    use: yay
    update_cache: true
    upgrade: true
    aur_only: true
  register: yay_result
  notify: reboot

- debug:
    var: yay_result
  when: yay_result

- name: Flush handlers
  meta: flush_handlers
