- name: Upgrade packages
  become: true
  apt:
    update_cache: true
    upgrade: full
  notify: reboot

- name: Flush handlers
  meta: flush_handlers

- name: Clean up dependencies
  become: true
  apt:
    autoremove: true
    purge: true
  notify: reboot

- name: Flush handlers
  meta: flush_handlers
