This repo has been retired and then un-retired twice already.
I primarily keep it for the name.

The old retired version (with the badly-structured Ansible project) is in
branch `old`.

The slightly less old version used to run GitHub Actions workflows is in branch
`old2`.
